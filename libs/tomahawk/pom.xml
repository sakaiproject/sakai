<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.sakaiproject</groupId>
        <artifactId>libs</artifactId>
        <version>27-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>org.sakaiproject.tomahawk</groupId>
    <artifactId>tomahawk</artifactId>
    <packaging>jar</packaging>

    <name>Sakai Tomahawk</name>
    <description>Apache MyFaces Tomahawk component library (vendored for Sakai, migrated to jakarta)</description>
    <url>https://myfaces.apache.org/tomahawk</url>

    <!--
      No deploy.target: unlike the Pluto container, Tomahawk is a JSF component library that
      belongs in each consuming webapp's WEB-INF/lib, exactly as the upstream tomahawk21 jar
      was used. Deploying it to shared as well would have Faces process its faces-config.xml
      and TLDs twice.
    -->

    <!--
        Upstream Tomahawk ships org.apache.myfaces.shared_tomahawk.* bundled alongside the
        component classes in a single artifact; this module reproduces that shape rather than
        splitting them. Dependencies are added with the source import - see README.md.
    -->
    <dependencies>
        <!--
          Tomahawk and Sakai's UIComponentTag-based tags both need Jakarta Faces 3.0, the last
          spec version retaining JSP tag support. Tracks master's property rather than pinning
          a literal, so this module follows the platform once master moves off Faces 4.
        -->
        <dependency>
            <groupId>jakarta.faces</groupId>
            <artifactId>jakarta.faces-api</artifactId>
            <version>${sakai.jakarta.faces-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.el</groupId>
            <artifactId>jakarta.el-api</artifactId>
            <version>${sakai.jakarta.el-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet.jsp</groupId>
            <artifactId>jakarta.servlet.jsp-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- Portlet 1.0, matching the vendored Pluto container (javax.portlet, deliberately not jakarta) -->
        <dependency>
            <groupId>javax.portlet</groupId>
            <artifactId>portlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- Source-retention annotations that drove upstream's TLD/faces-config generation -->
        <dependency>
            <groupId>org.apache.myfaces.buildtools</groupId>
            <artifactId>myfaces-builder-annotations</artifactId>
            <version>1.0.9</version>
            <scope>provided</scope>
        </dependency>
        <!-- Sakai excludes commons-logging tree-wide; jcl-over-slf4j supplies the JCL API -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-validator</groupId>
            <artifactId>commons-validator</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-beanutils</groupId>
            <artifactId>commons-beanutils</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
        </dependency>
        <!-- fileupload2 replaces the javax-only commons-fileupload 1.x upstream used -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-fileupload2-core</artifactId>
            <version>${sakai.commons.fileupload.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-fileupload2-jakarta-servlet6</artifactId>
        </dependency>
        <!-- Only used by the captcha component; xmlParserAPIs would shadow the JDK's JAXP -->
        <dependency>
            <groupId>batik</groupId>
            <artifactId>batik-awt-util</artifactId>
            <version>1.6-1</version>
            <exclusions>
                <exclusion>
                    <groupId>xml-apis</groupId>
                    <artifactId>xmlParserAPIs</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet.jsp.jstl</groupId>
            <artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
            <version>${sakai.jakarta.jstl-api.version}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>jakarta.servlet.jsp.jstl</artifactId>
        </dependency>
    </dependencies>

    <!-- Tomahawk keeps the standard Maven layout, not Sakai's src/java default -->
    <build>
        <sourceDirectory>src/main/java</sourceDirectory>
        <testSourceDirectory>src/test/java</testSourceDirectory>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
            </testResource>
        </testResources>
    </build>

</project>
