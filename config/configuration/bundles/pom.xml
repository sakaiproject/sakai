<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>config</artifactId>
		<groupId>org.sakaiproject</groupId>
		<version>12.3</version>
		<relativePath>../../pom.xml</relativePath>
	</parent>
	<name>configuration-bundles</name>
	<groupId>org.sakaiproject</groupId>
	<artifactId>configuration-bundles</artifactId>
	<organization>
		<name>Sakai Foundation</name>
		<url>http://sakaiproject.org/</url>
	</organization>
	<inceptionYear>2003</inceptionYear>
	<packaging>jar</packaging>
	<properties>
		<deploy.target>shared</deploy.target>
		<maven.resources.escapeString>\</maven.resources.escapeString>
		<!-- These can be overwritten by profiles and the profiles allow setting from
		  the command line, eg mvn -Dlocal.service=MySakaiService install -->
		<version.sakai>${sakai.version}</version.sakai>
		<version.service>${buildNumber}</version.service>
	</properties>
	<profiles>
		<profile>
			<id>cstm-service</id>
			<activation>
				<property>
					<name>local.service</name>
				</property>
			</activation>
			<properties>
				<version.service>${local.service}</version.service>
			</properties>
		</profile>
		<profile>
			<id>cstm-sakai</id>
			<activation>
				<property>
					<name>local.sakai</name>
				</property>
			</activation>
			<properties>
				<version.sakai>${local.sakai}</version.sakai>
			</properties>
		</profile>
	</profiles>
	<build>
		<resources>
			<resource>
				<directory>${basedir}/src/bundle</directory>
				<includes>
					<include>**/auto.sakai.properties</include>
					<include>**/default.sakai.properties</include>
				</includes>
				<filtering>true</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>buildnumber-maven-plugin</artifactId>
				<version>1.4</version>
				<executions>
					<execution>
						<phase>validate</phase>
						<goals>
							<goal>create</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<shortRevisionLength>8</shortRevisionLength>
					<revisionOnScmFailure>Sakai</revisionOnScmFailure>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.6</version>
				<executions>
					<execution>
						<phase>process-resources</phase>
						<configuration>
							<target>
								<concat destfile="${project.build.outputDirectory}/org/sakaiproject/config/bundle/default.sakai.properties" append="true">
									<fileset dir="${project.build.outputDirectory}" includes="**/auto.sakai.properties" />
								</concat>
							</target>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<configuration>
					<delimiters>
						<delimiter>$${*}</delimiter>
					</delimiters>
					<useDefaultDelimiters>false</useDefaultDelimiters>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
