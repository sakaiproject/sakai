-- Update the role property table to support provider-only roles

ALTER TABLE SAKAI_REALM_ROLE_DESC ADD PROVIDER_ONLY CHAR(1) NULL;
-- Update the role property table to support provider-only roles

ALTER TABLE SAKAI_REALM_ROLE_DESC ADD PROVIDER_ONLY CHAR(1) NULL;
