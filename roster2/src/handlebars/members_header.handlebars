<div id="roster-members-header" class="roster-table">
    <div class="roster-table-header-group">
        <div class="roster-table-row">
            {{#if viewPicture}}
            <div class="roster-table-cell">{{tr 'roster' 'facet_picture'}}</div>
            {{/if}}
            {{#unless hideNames}}
            <div class="roster-table-cell">{{tr 'roster' 'facet_name'}}</div>
            {{/unless}}
            {{#if viewUserDisplayId}}
            <div class="roster-table-cell">{{tr 'roster' 'facet_userId'}}</div>
            {{/if}}
            {{#if enrollmentsMode}}
            <div class="roster-table-cell">{{tr 'roster' 'facet_status'}}</div>
            <div class="roster-table-cell">{{tr 'roster' 'facet_credits'}}</div>
            {{else}}
            <div class="roster-table-cell">{{tr 'roster' 'facet_role'}}</div>
            {{/if}}
            {{#if showVisits}}
                {{#if ../viewSiteVisits}}
            <div class="roster-table-cell">{{tr 'roster' 'total_visits'}}</div>
            <div class="roster-table-cell">{{tr 'roster' 'last_visit'}}</div>
                {{/if}}
            {{/if}} <!-- showVisits -->
            {{#if viewGroup}}
            <div class="roster-table-cell roster-group-cell">{{tr 'roster' 'facet_groups'}}</div>
            {{/if}}
            {{#if viewEmail}}
            <div class="roster-table-cell">{{tr 'roster' 'facet_email'}}</div>
            {{/if}}
            {{#if viewUserProperty}}
            <div class="roster-table-cell">{{translate 'facet_userProperties'}}</div>
            {{/if}}
        </div>
    </div>
    <div id="roster-members" class="roster-table-row-group"></div>
</div>
