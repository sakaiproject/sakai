<?xml version="1.0" encoding="UTF-8"?>

<!-- default goal for the project builds and installs the war in the local repository -->
<project default="full"
   xmlns:ant="jelly:ant">
<!--
	<goal name="sakai:sakai.build" >
		<attainGoal name="jar:install" />
	</goal>

	<goal name="java:compile">
	</goal>
	
	<goal name="jar:jar" >
    		<ant:ant dir="${basedir}" antfile="build.xml" target="jar" />
		<copy tofile="${maven.build.dir}/${maven.final.name}.jar" file="lib/radeox.jar" />
	</goal>
	
	<goal name="clean" >
		<!- - invoke the ant clean target - ->
    		<ant:ant dir="${basedir}" antfile="build.xml" target="clean" />
	</goal>

-->

</project>
