The "opt" directory is a basic skeleton of the configuration files necessary to get Navigo configured and running.  
If this is your first time installing Navigo, you will need to copy "opt" to the root of the file system (i.e. "C:\" for Windows and "/" for UNIX).

Further documentation pending.